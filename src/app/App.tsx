import { Routing } from "./providers/routing/ui/Routing";

// TODO: fix tag_list,  fix double openned todo_detail, adaptation, add board view

function App() {
  return <Routing />;
}

export default App;
